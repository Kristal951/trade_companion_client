const AuthHeader = ({ title, subtitle }) => (
  <div className="text-center mb-8">
    <h1 className="text-3xl font-bold text-primary mb-2">{title}</h1>
    <p className="text-mid-text">{subtitle}</p>
  </div>
);

export default AuthHeader;
